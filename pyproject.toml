[project]
name = "linear-python"
version = "0.1.0"
description = "Pragmatic Python client for the Linear GraphQL API"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
authors = [
    { name = "ewinter-hackerone", email = "ewinter@hackerone.com" }
]
requires-python = ">=3.14"
keywords = ["linear", "linear.app", "graphql", "api", "client", "sdk"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]
dependencies = [
    "httpx>=0.27",
    "pydantic>=2.7",
]

[project.urls]
Homepage = "https://github.com/Hacker0x01/linear-python"
Documentation = "https://hacker0x01.github.io/linear-python/"
Repository = "https://github.com/Hacker0x01/linear-python"
Issues = "https://github.com/Hacker0x01/linear-python/issues"

[build-system]
requires = ["uv_build>=0.11.19,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "respx>=0.21",
    "ruff>=0.6",
]
docs = [
    "mkdocs>=1.6",
    "mkdocs-material>=9.5",
    "mkdocstrings[python]>=0.26",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=linear_python --cov-report=term-missing --cov-fail-under=90"

[tool.coverage.run]
branch = true
source = ["linear_python"]

[tool.coverage.report]
show_missing = true
# The optional live smoke test only runs when LINEAR_API_KEY is set.
exclude_also = ["if __name__ == .__main__.:"]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
